Я работаю на MacOS. Команды представлены для Unix подобных систем


## Task 1

```bash
% cd task1
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install cowsay
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 2

```bash
% cd task2
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install requests
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 3

```bash
% cd task3
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install pandas
(.venv) % pip freeze > requirements.txt
(.venv) % touch data.csv
    *write info to data.csv*
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 4

```bash
% cd task4
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install numpy
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 5

```bash
% cd task5
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install pillow
(.venv) % pip freeze > requirements.txt
(.venv) % cp /PATH/TO/IMG/input.jpg .
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 6

```bash
% cd task6
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install art
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 7

```bash
% cd task7
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install asciimatics
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 8

```bash
% cd task8
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install pyephem
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 9

```bash
% cd task9
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install pygame
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```

## Task 10

```bash
% cd task10
% python -m venv .venv
% source .venv/bin/activate
(.venv) % pip install pyjokes
(.venv) % pip freeze > requirements.txt
(.venv) % touch main.py
    *write code to main.py*
(.venv) % python main.py
    *programm result*
(.venv) % deactivate
```